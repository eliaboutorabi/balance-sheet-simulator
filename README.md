# Balance Sheet Simulator

A Vite + React simulator for entering assets, liabilities, and equity, then instantly checking whether the accounting equation balances.

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The repository includes a GitHub Actions workflow that builds the app and deploys it to GitHub Pages.
